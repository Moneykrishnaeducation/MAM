# MAM

Python backend generated by Better Fullstack.

## Development

Install dependencies:

```sh
uv sync --extra dev
```

Run the application:

```sh
uv run python -m app.main
```

Production with Gunicorn:

```sh
uv run gunicorn app.main:application
```

The health endpoint is available at `/health`.
